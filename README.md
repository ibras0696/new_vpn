# VPPPN — Telegram‑бот для управления VLESS‑ключами

Бот выдаёт, показывает и отзывает VLESS‑ключи для XRay, автоматически обновляет конфигурацию и синхронизирует состояние при запуске. Вся работа ведётся через Telegram‑интерфейс с инлайн‑кнопками, доступен только администратору.

## Возможности
- создание VLESS‑ключей с QR‑кодом и ссылкой одним нажатием;
- предустановленные сроки действия + ввод произвольного срока;
- список и удаление ключей из одной панели;
- автоматическая регистрация/удаление клиентов в XRay через API;
- планировщик очистки просроченных ключей;
- health‑check XRay и повторная синхронизация активных ключей при старте бота.

## Требования
- Python 3.11+
- XRay Core (CLI `xray`)
- SQLite (по умолчанию) или PostgreSQL
- Telegram Bot API токен

## Быстрый старт (локально)
```bash
python3 -m venv .venv
source .venv/bin/activate
make dev-install             # установка зависимостей
cp .env.example .env         # создай .env и заполни токены/настройки
                            # (шаблон перечисляет обязательные переменные)
make run                     # запуск бота
```

## Настройки окружения
Основные переменные задаются в `.env`:

| Переменная | Значение |
|-----------|----------|
| `BOT_TOKEN` | Токен Telegram‑бота |
| `ADMIN_ID` | Telegram ID администратора |
| `DB_ENGINE` | `sqlite+aiosqlite` или `postgresql+asyncpg` |
| `DB_NAME` | Путь к файлу SQLite или имя БД PostgreSQL |
| `XRAY_DOMAIN` | Домен сервера VLESS |
| `XRAY_PORT` | Порт входящего соединения |
| `XRAY_SECURITY` | `tls` или `none` |
| `XRAY_NETWORK` | `tcp`, `ws` или `grpc` |
| `XRAY_API_ENABLED` | `true`, чтобы использовать API XRay |
| `XRAY_API_PORT` | Порт API (по умолчанию `10085`) |
| `XRAY_INBOUND_TAG` | Тег inbound’а, к которому привязываются клиенты |

> ⚠️ Файл `.env` добавлен в `.gitignore`, поэтому храни его на сервере вручную.

## Docker
```bash
make docker-build           # сборка образа
docker compose up -d        # запуск сервиса
docker compose logs -f      # просмотр логов
```

Dockerfile подтягивает XRay CLI (по умолчанию `25.10.15`) внутрь контейнера, чтобы бот мог управлять клиентами через API. При необходимости можно переопределить версию через `--build-arg XRAY_VERSION=...`.

## Гайд по установке на Ubuntu 24.04 LTS

### 1. Подготовка сервера
Запусти автоматический скрипт (потребуются root-права):
```bash
sudo python3 scripts/setup_ubuntu.py --admin vpppn --ports 443 10085
```

Скрипт:
- обновит систему и поставит `curl`, `git`, `ufw`, `unzip`;
- установит Docker Engine и compose plugin;
- создаст пользователя `vpppn` и добавит его в группы `sudo` и `docker`;
- откроет указанные порты через UFW (по умолчанию 443 и 10085).

Если хочешь проверить действия, добавь `--dry-run` — команды будут выведены, но не выполнены. Отдельные шаги можно отключить флагами, например `--skip-docker`, если движок уже стоит.

### 2. Брандмауэр
Если пропустил скрипт, открой порты вручную:
```bash
sudo ufw allow OpenSSH
sudo ufw allow 443/tcp          # или другой порт VLESS
sudo ufw allow 10085/tcp        # XRay API для бота
sudo ufw enable
```

### 3. Установка Docker и Docker Compose
Скрипт `setup_ubuntu.py` ставит Docker автоматически. Если запускал вручную:
```bash
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER
```

### 4. Установка XRay Core (сервис)
1. Скачай бинарь:
    ```bash
    sudo mkdir -p /etc/xray /var/log/xray
    XRAY_VERSION=25.10.15
    sudo curl -L "https://github.com/XTLS/Xray-core/releases/download/v${XRAY_VERSION}/Xray-linux-64.zip" -o /tmp/xray.zip
    sudo apt install -y unzip && sudo unzip /tmp/xray.zip -d /usr/local/share/xray
    sudo install -m 755 /usr/local/share/xray/xray /usr/local/bin/xray
    ```

2. Сгенерируй systemd unit:
    ```bash
    sudo python3 scripts/install_xray_service.py \
        --user root \
        --exec /usr/local/bin/xray \
        --config /etc/xray/config.json
    ```

   Скрипт создаст unit-файл, перезагрузит systemd и запустит сервис. Используй `--dry-run`, чтобы просто посмотреть содержимое.

   Дополнительные флаги:
   - `--user` — под кем запускать XRay (по умолчанию `root`).
   - `--unit-path` — куда сохранить unit, если нужно своё имя.
   - `--log-dir` — каталог для логов (создаётся автоматически).

> ⚠️ XRay стартует даже с пустым config.json. После первого запуска бота файл перезапишется.

### 5. Развёртывание бота
```bash
cd /opt
sudo git clone https://github.com/<your-account>/vpppn.git
sudo chown -R vpppn:vpppn vpppn
```

Создай `.env` на основе шаблона:
```bash
sudo -u vpppn cp /opt/vpppn/.env.example /opt/vpppn/.env
sudo -u vpppn nano /opt/vpppn/.env
```

Заполни реальные токены и параметры сервера.

### 6. Первый запуск и генерация конфига
```bash
sudo -u vpppn docker compose -f /opt/vpppn/docker-compose.yml up -d --build
```

После старта в логах появится сообщение `✅ XRay config.json создан...`; проверь файл:
```bash
sudo jq . /etc/xray/config.json
```

Перезапусти systemd‑сервис XRay, чтобы применить конфиг:
```bash
sudo systemctl restart xray
```

### 7. Проверка
```bash
docker compose -f /opt/vpppn/docker-compose.yml ps
docker compose -f /opt/vpppn/docker-compose.yml logs -f bot
sudo systemctl status xray
```

Зайди в Telegram, отправь боту `/start` с администратора — бот покажет меню. Создай тестовый ключ, убедись что он появился в `/etc/xray/config.json`.

## Полезные команды
- `make run` — локальный запуск бота.
- `make lint` / `make format` — проверки Ruff.
- `make test` — PyTest (включает негативные сценарии API XRay).
- `make clean` — очистка кешей и артефактов.

## Скрипты автоматизации
- `scripts/setup_ubuntu.py` — готовит «чистый» Ubuntu 22.04/24.04: ставит Docker, настраивает UFW и создаёт системного пользователя. Запускай с `sudo`, смотри флаги `--admin`, `--ports`, `--skip-docker`, `--dry-run`.
- `scripts/install_xray_service.py` — разворачивает systemd‑юнит для XRay. Принимает параметры `--user`, `--exec`, `--config`, `--unit-path`, `--log-dir`; флаг `--dry-run` покажет результат без записи.

## Структура проекта
```
handlers/         # Telegram-хендлеры и клавиатуры
services/         # XRay интеграция, планировщик, клиенты
data/             # ORM, модели и репозитории
filters/          # Пользовательские фильтры aiogram
config.py         # Настройки (pydantic-settings)
main.py           # Точка входа бота
docker-compose.yml / Dockerfile
```

## Отладка и обслуживание
- При недоступности XRay API бот логирует предупреждение, но продолжает работу; при появлении связи достаточно выполнить `/menu → Список ключей`, чтобы убедиться в синхронизации.
- Планировщик каждые 10 минут помечает просроченные ключи и пытается удалить их из XRay. Если API не отвечает — действие повторится в следующем цикле.
- После обновления бота перезапусти контейнер (`docker compose restart bot`) и XRay (`sudo systemctl restart xray`), чтобы убедиться, что конфиг и клиенты актуальны.

## Тестирование
В проекте настроены unit‑тесты для критичных точек взаимодействия с XRay CLI:
```bash
pytest
```

Добавляй новые тесты, если расширяешь функциональность клавиатур или репозитория ключей.

---

Если заметишь неточность в инструкции или хочешь добавить автоматизацию (systemd unit для бота, роль Ansible, восстановление клиентов после рестарта XRay) — создай issue или pull request. Удачного развёртывания! 🚀
