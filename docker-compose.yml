version: "3.9"

services:
  bot:
    build:
      context: .
    image: vpppn-bot:latest
    env_file:
      - .env
    command: ["python", "-m", "main"]
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./etc/xray:/etc/xray
